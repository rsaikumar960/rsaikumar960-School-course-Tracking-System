@{
    ViewData["Title"] = "Home Page";
}

<style>
    .custom-heading {
        padding: 20px;
        margin-bottom: 40px;
        background: linear-gradient(to right, #e0f7fa, #ffffff);
        border-radius: 15px;
        color: #0d6efd;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
        font-weight: 700;
        letter-spacing: 1px;
    }

    /* Optional: If you want to ensure the image fills the container */
    .img-container {
        width: 100%;
        height: 600px;
        background-size: cover;
        background-position: center;
    }
</style>

<div class="img-container" style="background-image: url('/Images/im2.jpg');">
    <h1 class="display-4 text-center custom-heading">
        Sri Chaithanya
    </h1>
</div>
